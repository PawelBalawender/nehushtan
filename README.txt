Date: January 2019
Just Snake game in matplotlib
It makes use of the `threading` library to async. take input from user
Also, the GUI is implemented using `matplotlib.patches` module

Usage:
$ python37 main.py

